server:
    port: 8084
    address: localhost
    servlet:
        context-path: /toprated
spring:
    datasource:
        type: com.zaxxer.hikari.HikariDataSource
        url: jdbc:postgresql://localhost:5434/toprated
        username: toprated
        password:
    jpa:
        properties:
            hibernate.temp.use_jdbc_metadata_defaults: false